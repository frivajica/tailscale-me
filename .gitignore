# Build artifacts — the exe embeds the auth key, never commit it
/dist/
/TailscaleMe.exe
*.exe
*.syso
/tmp/

# Secrets — never commit a live auth key
.authkey

# Logs
*.log

# OS junk
.DS_Store
Thumbs.db
Desktop.ini